language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - hhvm

before_install:
  - sudo apt-get update -qq
  # Install latest uptime
  - sudo apt-get install procps

install:
  # Install shellcheck
  - sudo ./test/build/install.sh
  # Install webstatus
  - sudo ./bin/install.sh -y

script:
  - ./test/build/build.sh
